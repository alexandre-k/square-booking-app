// import { useContext } from "react";
// import StoreLocationProvider from "context/LocationProvider";

const useLocation = () => {
//     const context = useContext<LocationState>(StoreLocationProvider)
//     if (context === undefined) throw new Error("Context must be used within a provider");
//     return context;
}

export default useLocation;
